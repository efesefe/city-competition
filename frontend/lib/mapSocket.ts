/**
 * @deprecated Import from `@/lib/realtimeSocket` instead.
 * Re-exports kept so older imports keep working during Track A.
 */
export {
  connectMapSocket,
  connectRealtimeSocket,
  type MapBBox,
  type MapSocketEvent,
  type MapSocketHandle,
  type MapSocketOptions,
  type MapSocketStatus,
  type RealtimeSocketEvent,
  type RealtimeSocketHandle,
  type RealtimeSocketOptions,
  type RealtimeSocketStatus,
  type SupportAppliedMessage,
  type WalletBalanceChangedMessage,
} from "@/lib/realtimeSocket";
